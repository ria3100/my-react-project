import React from 'react'

import { Layout } from 'antd'
const { Header } = Layout

export default class extends React.Component {
  render() {
    return (
      <Header>
        <h2>foo</h2>
      </Header>
    )
  }
}
